# Docs Publishing

The documentation site lives at `docs/` and uses VitePress.

## Commands

```bash
cd docs
npm install
npm run docs:build
npm run docs:preview
```

Documentation must evolve with code changes. Architecture, API, and development guides are first-class project assets.
