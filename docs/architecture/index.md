# 架构入口

仓库级工程记忆、架构执行基线、前后端技术方案和功能设计，现已统一收敛到仓库根目录的 `agents.md`。

本目录只保留面向领域和专题的公共架构文档，不再重复维护以下工程记忆内容：

- 系统分层总览
- 前端工程结构总览
- 后端工程结构总览
- 工程变更规则与记忆更新规则

## 当前文档边界

这里保留：

- 主数据目录
- 应用交付
- 监控与告警
- AI Copilot
- Agent 协议
- 权限模型
- 多集群模型
- 事件模型
- 审计模型
- MCP 集成

工程实施时，请优先以仓库根目录 `agents.md` 为准；专题设计与领域约束，以本目录下对应文档为准。

补充约定：

- `业务线`
- `交付环境`
- `应用环境绑定`

在当前信息架构中属于独立的主数据目录，不再以“应用交付的专属子域”呈现；但其权限键与后端 API 仍沿用既有 `delivery.*` 命名，直到后续权限语义重构专门收敛。
