interface:
  display_name: "Soha Deploy"
  short_description: "Docker, Compose, raw Kubernetes, and embedded web"
  default_prompt: "Use $soha-deploy to update and verify a Soha core deployment artifact."
