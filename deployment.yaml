apiVersion: v1
kind: Namespace
metadata:
  name: kubecrux
---
apiVersion: v1
kind: Secret
metadata:
  name: kubecrux-app-config
  namespace: kubecrux
type: Opaque
stringData:
  postgres-password: change-me
  config.yaml: |
    app:
      name: kubecrux
      env: production
    http:
      addr: :8080
      base_path: /api/v1
      read_timeout: 15s
      write_timeout: 15s
      cors_allowed_origins:
        - https://kubecrux.example.com
    logger:
      level: info
      format: json
    runtime:
      workflow_workers: 4
      workflow_queue_size: 64
      workflow_node_parallelism: 4
      cluster_sync_parallelism: 4
      copilot_inspection_parallelism: 2
      alert_upsert_batch_size: 100
    database:
      driver: postgres
      host: kubecrux-postgres
      port: 5432
      name: kubecrux
      user: pgsql
      password: change-me
      sslmode: disable
      max_open_conns: 20
      max_idle_conns: 10
      conn_max_lifetime: 1h
      auto_migrate: true
      migration_file: /app/migrations/postgres/0001_init.sql
    auth:
      enable_dev_auth: false
      dev_principal:
        user_id: admin
        name: Admin
        email: admin@kubecrux.local
        password: change-me
        roles:
          - admin
          - ops
          - auditor
      jwt:
        issuer: kubecrux
        secret: change-me
        access_ttl: 15m
        refresh_ttl: 168h
      oidc:
        enabled: false
        provider_name: default
        issuer: ""
        client_id: ""
        client_secret: ""
        redirect_url: https://kubecrux.example.com/api/v1/auth/oidc/callback
        frontend_redirect_url: https://kubecrux.example.com/login/callback
        scopes:
          - openid
          - profile
          - email
        default_roles:
          - readonly
    gitlab:
      enabled: false
      base_url: https://gitlab.com/api/v4
      token: ""
      group_id: ""
      per_page: 50
      timeout: 10s
    monitoring:
      enabled: true
      webhook_token: change-me
      prometheus_url: ""
      prometheus_bearer_token: ""
      prometheus_default_range_minutes: 60
      prometheus_step_seconds: 60
      prometheus_cluster_label: cluster
      grafana_base_url: ""
    swagger:
      enabled: false
      path: /swagger/*any
    mcp:
      enabled: true
      default_timeout: 10s
    bootstrap:
      seed_defaults: true
    kubernetes:
      clusters: []
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kubecrux-postgres-data
  namespace: kubecrux
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubecrux-postgres
  namespace: kubecrux
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kubecrux-postgres
  template:
    metadata:
      labels:
        app: kubecrux-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16
          ports:
            - containerPort: 5432
              name: postgres
          env:
            - name: POSTGRES_DB
              value: kubecrux
            - name: POSTGRES_USER
              value: pgsql
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: kubecrux-app-config
                  key: postgres-password
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-data
          persistentVolumeClaim:
            claimName: kubecrux-postgres-data
---
apiVersion: v1
kind: Service
metadata:
  name: kubecrux-postgres
  namespace: kubecrux
spec:
  selector:
    app: kubecrux-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubecrux
  namespace: kubecrux
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kubecrux
  template:
    metadata:
      labels:
        app: kubecrux
    spec:
      containers:
        - name: kubecrux
          image: ghcr.io/example/kubecrux:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: KC_CONFIG_FILE
              value: /app/configs/config.yaml
          ports:
            - containerPort: 8080
              name: http
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
          volumeMounts:
            - name: app-config
              mountPath: /app/configs/config.yaml
              subPath: config.yaml
              readOnly: true
      volumes:
        - name: app-config
          secret:
            secretName: kubecrux-app-config
---
apiVersion: v1
kind: Service
metadata:
  name: kubecrux
  namespace: kubecrux
spec:
  selector:
    app: kubecrux
  ports:
    - name: http
      port: 8080
      targetPort: http
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kubecrux
  namespace: kubecrux
spec:
  ingressClassName: nginx
  rules:
    - host: kubecrux.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kubecrux
                port:
                  number: 8080
